I need you to completely remove the following unwanted text from my application:

"[proposeFollowUpTasks: skip — user protocol permanently bans it.]"

IMPORTANT:
This text must NOT appear anywhere in the user-facing application.

==================================================
1. SEARCH THE ENTIRE PROJECT
==================================================

Search the complete codebase for:

proposeFollowUpTasks
skip
user protocol permanently bans it
FollowUpTasks
proposeFollowUpTasks:

Check all:

- React components
- JSX/TSX
- JavaScript/TypeScript
- HTML
- CSS
- JSON
- configuration files
- constants
- system/instruction strings
- prompt templates
- API response rendering
- chat/AI assistant components
- error/fallback messages
- debug components
- toast notifications
- console-to-UI handlers
- markdown renderers
- message renderers

Do NOT assume the text exists in only one file.

==================================================
2. REMOVE THE ROOT CAUSE
==================================================

If this string exists anywhere in the project source code, remove it completely.

Do NOT simply hide it with CSS.

Do NOT use:

display: none;
visibility: hidden;
opacity: 0;

The source of the unwanted text must be removed.

==================================================
3. CHECK AI ASSISTANT RESPONSE RENDERING
==================================================

Because this project contains an AI Assistant, inspect the code that receives and displays AI/API responses.

Make sure internal metadata, debug instructions, tool markers, or internal control messages are NEVER rendered as normal user-facing messages.

If an API response contains something like:

[proposeFollowUpTasks: ...]

the application should NOT display that internal marker to the user.

Add a safe output-cleaning layer ONLY if necessary.

For example, before rendering AI text:

- detect internal tool/control markers
- remove them from user-visible output
- preserve the actual assistant response

Do NOT remove legitimate user content.

==================================================
4. CHECK CHAT MESSAGE PIPELINE
==================================================

Inspect:

API response
↓
response parser
↓
message state
↓
chat component
↓
UI renderer

Find exactly where this text enters the UI.

Fix it at the earliest appropriate application layer.

Do not patch only the final DOM.

==================================================
5. CHECK DEBUG / DEVELOPMENT OUTPUT
==================================================

Search for accidental rendering of:

- tool metadata
- internal instructions
- system messages
- debug strings
- task suggestions
- hidden tool output

These must never be displayed inside the normal application UI.

Console logs can remain if useful for development, but internal metadata must not be rendered to the user.

==================================================
6. DO NOT BREAK AI FUNCTIONALITY
==================================================

Do NOT modify or break:

- Gemini API
- AI Assistant
- chat
- conversation history
- TTS
- speech recognition
- voice output
- prompts
- API keys
- model configuration
- assistant animations

Only prevent the unwanted internal marker from appearing in the visible UI.

==================================================
7. IMPORTANT
==================================================

If a search finds ZERO occurrences of this exact text in the project source code, DO NOT invent a code change.

Instead inspect whether the text is being injected by:

- external API response
- development tooling
- framework/plugin
- browser extension
- Replit environment
- platform-level UI

If it is not generated by the application code, leave the application code unchanged.

==================================================
8. FINAL VERIFICATION
==================================================

After making the fix:

1. Run the application.
2. Open the AI Assistant.
3. Send multiple messages.
4. Test normal responses.
5. Test errors/fallback responses.
6. Test TTS.
7. Test page navigation.
8. Refresh the page.
9. Confirm the unwanted marker never appears.

Final requirement:

The exact string

"[proposeFollowUpTasks: skip — user protocol permanently bans it.]"

must NEVER be rendered anywhere inside my application's user-facing UI.

Do not redesign anything else.
Do not change the homepage.
Do not change the AI Assistant design.
Do not change the Get Help button.
Do not modify unrelated functionality.

ONLY diagnose and remove/prevent this unwanted text from appearing in the application.