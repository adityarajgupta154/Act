# toast (chunk 2)

Source: `src/components/ui/toast.tsx` (3 importers) + `toaster.tsx` (1) — Radix toast, variant default/destructive.
Contract: keep Radix + shadcn API and the app's rounded-2xl friendly panel look; success tone green, error destructive; Toaster mounted in story (Toast + Toaster = one family story).
