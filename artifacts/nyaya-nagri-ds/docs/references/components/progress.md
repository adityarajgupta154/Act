# progress (chunk 2)

Source: no consumed primitive; app pattern is the authority: `w-full h-3 bg-slate-100 rounded-full overflow-hidden` + colored fill (ParentDashboard.tsx:44); XP/level bars in HUD use amber/gold fills with white rings.
Contract: Radix Progress (scaffold implementation) restyled: h-3 rounded-full track (muted), fill variants primary(orange)/gold(amber-500)/success(green)/sky; smooth width transition, reduced-motion safe.
