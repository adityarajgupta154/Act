# toggle (chunk 3)

Source: `src/components/ui/toggle.tsx` (1 importer) — Radix toggle, pressed state.
Contract: keep Radix + API; pill shape, pressed = navy chrome (#14306E) with white text per app's selected-state buttons; 44px min target.
