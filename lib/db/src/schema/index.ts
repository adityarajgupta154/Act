/**
 * Nyaya Nagri — Drizzle schema exports
 *
 * Table definitions that map 1:1 to the app's in-memory stores.
 * Run `pnpm --filter @workspace/db push` to apply to Supabase.
 */
export * from './players';
export * from './progress';
export * from './settings';
export * from './certificates';
